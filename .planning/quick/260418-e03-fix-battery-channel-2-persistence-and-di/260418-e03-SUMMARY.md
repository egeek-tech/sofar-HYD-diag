---
phase: quick
plan: 260418-e03
subsystem: hub
tags: [bugfix, battery, schema-broadcast, websocket]
dependency_graph:
  requires: []
  provides: [battery-schema-broadcast-on-reconfiguration]
  affects: [internal/hub/hub_streaming.go, internal/hub/hub.go, internal/hub/hub_test.go]
tech_stack:
  added: []
  patterns: [broadcastResultToSection-for-schema-updates]
key_files:
  created: []
  modified:
    - internal/hub/hub_streaming.go
    - internal/hub/hub.go
    - internal/hub/hub_test.go
decisions:
  - Used broadcastResultToSection (interface{}) instead of broadcastToSection (OutboundMessage) since SectionSchemaMessage is a standalone struct
  - Placed schema broadcast inside h.funcs callback (hub_streaming.go) to maintain hub event loop safety for subscriber access
metrics:
  duration: 7m52s
  completed: 2026-04-18T08:23:51Z
  tasks: 2/2
  files: 3
---

# Quick Task 260418-e03: Fix Battery Channel 2 UI Persistence Summary

Broadcast updated section_schema to all battery subscribers after channel reconfiguration (auto-detection or reconnect reset) so the frontend renders the correct channel skeleton.

## Task Results

| Task | Name | Type | Commit | Key Changes |
|------|------|------|--------|-------------|
| 1 | Broadcast updated schema after battery Groups rebuild | auto | a5aea47 | Added schema broadcast in hub_streaming.go (after auto-detection) and hub.go (after reconnect reset) |
| 2 | Add tests proving schema broadcast on reconfiguration and reconnect | auto/tdd | 76d32d4 | Extended TestBatteryBatchRead_AutoDetect and TestBatteryBatchRead_ReconnectResetsChannels with schema assertions |

## Changes Made

### Task 1: Schema Broadcast After Battery Groups Rebuild

Two targeted insertions following the existing `subscribeClient` pattern (lines 324-335 in hub.go):

**Location 1 -- hub_streaming.go (auto-detection callback):** After the `h.funcs` callback rebuilds `sec.Groups`, `sec.Probes`, `sec.BatchPlan`, and resets `SpanTracker`, a new `buildSectionSchema` + `broadcastResultToSection` call broadcasts the updated schema to all battery subscribers before `triggerSectionRead` fires the next read cycle. This runs inside the hub event loop (via h.funcs channel) so subscriber access is safe.

**Location 2 -- hub.go (reconnect reset):** After `handleStateEvent` resets battery to 2-channel default on `StateConnected`, the same schema broadcast pattern sends the 2-channel skeleton to all subscribers, ensuring the frontend does not retain a stale 4-channel layout until auto-detection runs again.

### Task 2: Integration Tests

Extended two existing tests to verify schema broadcast:

- **TestBatteryBatchRead_AutoDetect:** Changed from `waitForMessageType` to raw message collection. After section_complete, searches collected messages for a `section_schema` with a "Channel 4" group (proving 4-channel schema was broadcast, not just the initial 2-channel). Asserts 6 groups total (4 channels + Global Stats + Internal Info).

- **TestBatteryBatchRead_ReconnectResetsChannels:** Changed `drainClientMessages` to `drainRawMessages` after reconnect. Searches for a `section_schema` with 4 groups (2 channels + Global Stats + Internal Info) and asserts no "Channel 3" or "Channel 4" groups exist.

## Verification

- Both targeted tests pass: `TestBatteryBatchRead_AutoDetect` and `TestBatteryBatchRead_ReconnectResetsChannels`
- Full hub test suite passes with zero regressions (164.7s, all tests pass)
- Build compiles cleanly

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None.
